import { createStore } from 'vuex';

const store = createStore({
  state: {
    // Define tus propiedades de estado aquí
  },
  mutations: {
    // Define tus mutaciones aquí
  },
  actions: {
    // Define tus acciones aquí
  },
  getters: {
    // Define tus getters aquí
  }
});

export default store;

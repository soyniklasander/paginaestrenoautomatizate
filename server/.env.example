# Variables de entorno para el servidor (NestJS)
# Copia este archivo a .env y ajusta los valores según tu entorno.
PORT=3001
# Origen permitido para CORS en desarrollo
CORS_ORIGIN=http://localhost:3000
# Base de datos (si usas Postgres via docker-compose)
# Ajusta usuario/clave/DB según tu configuración
DATABASE_URL=postgresql://postgres:postgres@bd:5432/automatizate?schema=public
